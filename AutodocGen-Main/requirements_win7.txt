# AutoDocGen Win7 移植依赖清单
# 目标：Windows 7 SP1 + Python 3.8.20
# 安装命令：python -m pip install -r requirements_win7.txt --no-warn-script-location

# 文档生成核心
python-docx==1.1.2          # 高于 1.1.x 在 Py3.8 兼容性更好，1.2.x 也可
requests==2.32.3            # AI API 调用（Anthropic/OpenAI 兼容）

# 可选：tree-sitter 备用解析（如果想完全脱离 clangd）
# tree-sitter==0.21.3
# tree-sitter-c==0.21.3

# Qt GUI（PyQt5 5.15 是最后支持 Win7 的版本）
PyQt5==5.15.11
PyQt5-Qt5==5.15.2
PyQt5-sip==12.13.0

# PyInstaller 打包
pyinstaller==5.13.2         # Py3.8 兼容的最后版本
